# 📘 Полное руководство по настройке - Исправление ошибки удаления пользователей

## 🎯 Цель
Исправить ошибку **"Missing or insufficient permissions"** при удалении пользователей из workspace.

---

## 📦 Что нужно сделать (2 шага)

### ✅ Шаг 1: Развернуть правила Firestore
### ✅ Шаг 2: Добавить поле `isSuperAdmin: true` в документ пользователя

---

## 🔧 ШАГ 1: Развернуть правила Firestore

### Вариант A: Через Firebase Console (Рекомендуется - самый простой)

#### 1. Откройте Firebase Console
- Перейдите на [https://console.firebase.google.com/](https://console.firebase.google.com/)
- Войдите в свой аккаунт
- Выберите ваш проект

#### 2. Откройте Firestore Rules
- В левом меню: **Firestore Database** (иконка базы данных)
- Вкладка **"Rules"** (вверху)

#### 3. Скопируйте правила из файла
- Откройте файл **`firestore.rules`** в вашем проекте
- Выделите **ВСЁ** содержимое (Ctrl+A)
- Скопируйте (Ctrl+C)

#### 4. Вставьте в Firebase Console
- В редакторе правил удалите всё (Ctrl+A, Delete)
- Вставьте скопированные правила (Ctrl+V)

#### 5. Опубликуйте
- Нажмите кнопку **"Publish"** (Опубликовать) вверху справа
- Подождите несколько секунд
- Должно появиться сообщение об успешной публикации

#### 6. Проверка
- Кнопка "Publish" должна стать неактивной (серой)
- Правила должны содержать функцию `isSuperAdmin()` с тремя способами проверки

---

### Вариант B: Через Firebase CLI

#### 1. Установите Firebase CLI (если еще не установлен)
```bash
npm install -g firebase-tools
```

#### 2. Войдите в Firebase
```bash
firebase login
```

#### 3. Инициализируйте проект (если еще не инициализирован)
```bash
cd C:\Users\User\Documents\GitHub\Planer
firebase init firestore
```
- Выберите ваш проект
- Выберите файл правил: `firestore.rules`
- Нажмите Enter

#### 4. Задеплойте правила
```bash
firebase deploy --only firestore:rules
```

#### 5. Проверка
- Должно появиться "Deploy complete!"
- Проверьте в Firebase Console, что правила обновились

---

## 👤 ШАГ 2: Добавить поле `isSuperAdmin` в документ пользователя

### Вариант A: Через консоль браузера (Рекомендуется - быстрее)

#### 1. Откройте приложение
- Запустите ваше приложение
- Войдите в систему под аккаунтом `crazymhessel@gmail.com`

#### 2. Откройте консоль браузера
- Нажмите **F12** (или правой кнопкой → "Inspect" → "Console")
- Перейдите на вкладку **"Console"**

#### 3. Выполните скрипт
Скопируйте и вставьте этот код в консоль, затем нажмите Enter:

```javascript
(async () => {
  try {
    const { db, auth } = await import('./src/firebase');
    const { doc, updateDoc, getDoc } = await import('firebase/firestore');
    
    const user = auth.currentUser;
    if (!user) {
      console.error('❌ Пользователь не авторизован. Войдите в систему.');
      return;
    }
    
    console.log('👤 Текущий пользователь:', user.email);
    console.log('🆔 User ID:', user.uid);
    
    const userRef = doc(db, 'users', user.uid);
    const userDoc = await getDoc(userRef);
    
    if (!userDoc.exists()) {
      console.error('❌ Документ пользователя не найден в Firestore');
      console.log('💡 Создайте документ пользователя через Firebase Console или дождитесь автоматического создания при следующем входе.');
      return;
    }
    
    const userData = userDoc.data();
    console.log('📄 Текущие данные пользователя:', userData);
    
    if (userData.isSuperAdmin === true) {
      console.log('✅ Поле isSuperAdmin уже установлено в true');
      console.log('🔄 Все готово! Можете попробовать удалить пользователя.');
      return;
    }
    
    console.log('⏳ Добавляю поле isSuperAdmin...');
    await updateDoc(userRef, {
      isSuperAdmin: true
    });
    
    console.log('✅ Поле isSuperAdmin успешно добавлено!');
    console.log('🔄 Обновите страницу (F5) и попробуйте удалить пользователя снова.');
  } catch (error) {
    console.error('❌ Ошибка:', error);
    console.error('💡 Убедитесь, что:');
    console.error('   1. Вы авторизованы в системе');
    console.error('   2. Документ пользователя существует в Firestore');
    console.error('   3. У вас есть права на обновление документа');
  }
})();
```

#### 4. Проверка
- В консоли должно появиться: `✅ Поле isSuperAdmin успешно добавлено!`
- Обновите страницу (F5)

---

### Вариант B: Через Firebase Console (Визуально)

#### 1. Найдите ваш User ID
1. Firebase Console → **Authentication** → **Users**
2. Найдите пользователя с email `crazymhessel@gmail.com`
3. Скопируйте **UID** (User ID)

#### 2. Откройте документ пользователя
1. Firebase Console → **Firestore Database**
2. Коллекция **`users`**
3. Найдите документ с ID = ваш UID
4. Нажмите на документ

#### 3. Добавьте поле
1. Нажмите **"Add field"** (Добавить поле)
2. **Field name**: `isSuperAdmin`
3. **Type**: `boolean`
4. **Value**: `true`
5. Нажмите **"Update"**

#### 4. Проверка
- В документе должно появиться поле: `isSuperAdmin: true` (boolean)

---

## ✅ ШАГ 3: Проверка настройки

### 3.1. Проверьте правила Firestore
1. Firebase Console → Firestore → Rules
2. Убедитесь, что правила содержат функцию `isSuperAdmin()` с тремя способами проверки
3. Убедитесь, что правила опубликованы (кнопка "Publish" неактивна)

### 3.2. Проверьте документ пользователя
1. Firebase Console → Firestore → `users` → ваш документ
2. Убедитесь, что есть поле `isSuperAdmin: true` (boolean)
3. ИЛИ убедитесь, что поле `email` содержит `crazymhessel@gmail.com`

### 3.3. Проверьте в приложении
1. Обновите страницу приложения (F5)
2. Откройте консоль браузера (F12)
3. Попробуйте удалить пользователя из workspace
4. Смотрите логи в консоли:
   - Должны быть логи `[SettingsView]` и `[FirestoreService]`
   - Не должно быть ошибки "Missing or insufficient permissions"

---

## 📋 Чек-лист

Отметьте выполненные пункты:

- [ ] Правила Firestore развернуты через Firebase Console или CLI
- [ ] Правила содержат функцию `isSuperAdmin()` с тремя способами проверки
- [ ] Поле `isSuperAdmin: true` добавлено в документ пользователя
- [ ] Страница приложения обновлена (F5)
- [ ] Попробовал удалить пользователя - работает без ошибок

---

## 🐛 Устранение проблем

### Ошибка: "Missing or insufficient permissions"

**Проверьте:**
1. ✅ Правила Firestore развернуты? (Шаг 1)
2. ✅ Поле `isSuperAdmin: true` добавлено? (Шаг 2)
3. ✅ Страница обновлена? (F5)
4. ✅ Вы используете правильный аккаунт? (`crazymhessel@gmail.com`)

**Решение:**
- Повторите Шаг 1 и Шаг 2
- Обновите страницу (F5)
- Проверьте логи в консоли браузера

### Ошибка: "Документ пользователя не найден"

**Причина:**
- Документ создается автоматически при первом входе
- Или документ еще не создан

**Решение:**
- Войдите в систему в приложении (документ создастся автоматически)
- Или создайте вручную через Firebase Console

### Ошибка: "Пользователь не авторизован"

**Причина:**
- Вы не вошли в систему в приложении

**Решение:**
- Войдите в систему в приложении
- Убедитесь, что используете аккаунт `crazymhessel@gmail.com`

### Правила не применяются

**Причина:**
- Правила не опубликованы
- Или правила не совпадают с файлом `firestore.rules`

**Решение:**
1. Firebase Console → Firestore → Rules
2. Скопируйте содержимое `firestore.rules` из проекта
3. Вставьте в редактор правил
4. Нажмите "Publish"
5. Подождите несколько секунд

---

## 📁 Готовые файлы

### Правила Firestore
- **`firestore.rules`** - готовые правила для загрузки в Firebase Console
  - Содержит функцию `isSuperAdmin()` с тремя способами проверки
  - Поддерживает удаление пользователей супер-админами

### Скрипты
- **`scripts/setup-super-admin.js`** - скрипт для добавления `isSuperAdmin` через консоль браузера

---

## 📚 Дополнительные документы

- **`QUICK_START.md`** - быстрая настройка за 2 минуты
- **`SETUP_INSTRUCTIONS.md`** - подробная пошаговая инструкция
- **`SETUP_SUPER_ADMIN_SCRIPT.md`** - скрипты для автоматической настройки
- **`FIREBASE_CLI_SETUP.md`** - настройка через Firebase CLI
- **`CODE_ANALYSIS.md`** - анализ кода и последовательности
- **`PROBLEM_DIAGNOSIS.md`** - выводы и решение
- **`DEBUG_CHECKLIST.md`** - чек-лист для диагностики

---

## 🎉 Готово!

Если все пункты выполнены, удаление пользователей должно работать!

Если проблема сохраняется:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте правила в Firebase Console
3. Проверьте документ пользователя в Firestore
4. Смотрите `DEBUG_CHECKLIST.md` для детальной диагностики

---

## 💡 Важно

- Правила Firestore должны быть **опубликованы** (кнопка "Publish" в Firebase Console)
- Поле `isSuperAdmin: true` должно быть добавлено в документ пользователя
- Страница приложения должна быть **обновлена** (F5) после изменений
- Используйте аккаунт `crazymhessel@gmail.com` для тестирования

