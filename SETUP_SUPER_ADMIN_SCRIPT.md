# 🚀 Скрипт для автоматической настройки супер-админа

## Вариант 1: Через консоль браузера (Рекомендуется)

### Шаг 1: Откройте приложение
1. Запустите ваше приложение
2. Войдите в систему под аккаунтом `crazymhessel@gmail.com`

### Шаг 2: Откройте консоль браузера
1. Нажмите **F12** (или правой кнопкой → "Inspect" → "Console")
2. Перейдите на вкладку **"Console"**

### Шаг 3: Выполните скрипт

Скопируйте и вставьте следующий код в консоль, затем нажмите Enter:

```javascript
(async () => {
  try {
    // Динамический импорт модулей
    const { db, auth } = await import('./src/firebase');
    const { doc, updateDoc, getDoc } = await import('firebase/firestore');
    
    const user = auth.currentUser;
    if (!user) {
      console.error('❌ Пользователь не авторизован. Войдите в систему.');
      return;
    }
    
    console.log('👤 Текущий пользователь:', user.email);
    console.log('🆔 User ID:', user.uid);
    
    const userRef = doc(db, 'users', user.uid);
    const userDoc = await getDoc(userRef);
    
    if (!userDoc.exists()) {
      console.error('❌ Документ пользователя не найден в Firestore');
      console.log('💡 Создайте документ пользователя через Firebase Console или дождитесь автоматического создания при следующем входе.');
      return;
    }
    
    const userData = userDoc.data();
    console.log('📄 Текущие данные пользователя:', userData);
    
    // Проверяем, есть ли уже поле isSuperAdmin
    if (userData.isSuperAdmin === true) {
      console.log('✅ Поле isSuperAdmin уже установлено в true');
      console.log('🔄 Все готово! Можете попробовать удалить пользователя.');
      return;
    }
    
    // Обновляем документ
    console.log('⏳ Добавляю поле isSuperAdmin...');
    await updateDoc(userRef, {
      isSuperAdmin: true
    });
    
    console.log('✅ Поле isSuperAdmin успешно добавлено!');
    console.log('🔄 Обновите страницу (F5) и попробуйте удалить пользователя снова.');
  } catch (error) {
    console.error('❌ Ошибка:', error);
    console.error('💡 Убедитесь, что:');
    console.error('   1. Вы авторизованы в системе');
    console.error('   2. Документ пользователя существует в Firestore');
    console.error('   3. У вас есть права на обновление документа');
  }
})();
```

### Шаг 4: Проверка
- В консоли должно появиться: `✅ Поле isSuperAdmin успешно добавлено!`
- Обновите страницу (F5)
- Попробуйте удалить пользователя

---

## Вариант 2: Через Firebase Console (Визуально)

### Шаг 1: Найдите ваш User ID
1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Authentication** → **Users**
3. Найдите пользователя с email `crazymhessel@gmail.com`
4. Скопируйте **UID** (User ID)

### Шаг 2: Откройте документ пользователя
1. Перейдите в **Firestore Database**
2. Откройте коллекцию **`users`**
3. Найдите документ с ID = ваш UID
4. Нажмите на документ

### Шаг 3: Добавьте поле
1. Нажмите **"Add field"** (Добавить поле)
2. **Field name**: `isSuperAdmin`
3. **Type**: `boolean`
4. **Value**: `true`
5. Нажмите **"Update"**

---

## Вариант 3: Через Node.js скрипт (для разработчиков)

Создайте файл `setup-super-admin-node.js`:

```javascript
const admin = require('firebase-admin');
const serviceAccount = require('./path-to-service-account-key.json');

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

const db = admin.firestore();

async function setupSuperAdmin() {
  // Замените на ваш email
  const email = 'crazymhessel@gmail.com';
  
  // Найти пользователя по email
  const user = await admin.auth().getUserByEmail(email);
  console.log('Found user:', user.uid);
  
  // Обновить документ пользователя
  const userRef = db.collection('users').doc(user.uid);
  await userRef.set({
    isSuperAdmin: true
  }, { merge: true });
  
  console.log('✅ Super admin setup complete!');
  process.exit(0);
}

setupSuperAdmin().catch(console.error);
```

Запустите:
```bash
node setup-super-admin-node.js
```

**Примечание:** Требуется Service Account Key из Firebase Console.

---

## Проверка настройки

После выполнения любого из вариантов, проверьте:

1. **В Firebase Console:**
   - Firestore → `users` → ваш документ
   - Должно быть поле: `isSuperAdmin: true` (boolean)

2. **В приложении:**
   - Обновите страницу (F5)
   - Попробуйте удалить пользователя
   - Не должно быть ошибки "Missing or insufficient permissions"

---

## Устранение проблем

### Ошибка: "Пользователь не авторизован"
- Войдите в систему в приложении
- Убедитесь, что вы используете аккаунт `crazymhessel@gmail.com`

### Ошибка: "Документ пользователя не найден"
- Документ создается автоматически при первом входе
- Или создайте вручную через Firebase Console

### Ошибка: "Missing or insufficient permissions"
- Убедитесь, что правила Firestore развернуты
- Убедитесь, что поле `isSuperAdmin: true` добавлено
- Обновите страницу (F5)

