# 📋 Подробная инструкция по настройке Firestore

## 🎯 Цель
Исправить ошибку "Missing or insufficient permissions" при удалении пользователей.

---

## 📦 Шаг 1: Развернуть правила Firestore

### Вариант A: Через Firebase Console (Рекомендуется - самый простой)

#### 1.1. Откройте Firebase Console
1. Перейдите на [https://console.firebase.google.com/](https://console.firebase.google.com/)
2. Войдите в свой аккаунт Google
3. Выберите ваш проект

#### 1.2. Откройте Firestore Rules
1. В левом меню найдите **"Firestore Database"** (иконка базы данных)
2. Нажмите на него
3. Перейдите на вкладку **"Rules"** (вверху)

#### 1.3. Скопируйте правила из файла
1. Откройте файл `firestore.rules` в вашем проекте
2. Выделите **ВСЁ** содержимое (Ctrl+A)
3. Скопируйте (Ctrl+C)

#### 1.4. Вставьте правила в Firebase Console
1. В Firebase Console в редакторе правил удалите всё содержимое (Ctrl+A, Delete)
2. Вставьте скопированные правила (Ctrl+V)
3. Проверьте, что правила отображаются корректно

#### 1.5. Опубликуйте правила
1. Нажмите кнопку **"Publish"** (Опубликовать) вверху справа
2. Подождите несколько секунд
3. Должно появиться сообщение об успешной публикации

#### 1.6. Проверка
- Кнопка "Publish" должна стать неактивной (серой)
- Вверху должно быть сообщение "Rules published successfully" или подобное

---

### Вариант B: Через Firebase CLI (для продвинутых)

#### 1.1. Установите Firebase CLI
```bash
npm install -g firebase-tools
```

#### 1.2. Войдите в Firebase
```bash
firebase login
```
- Откроется браузер для авторизации
- Разрешите доступ

#### 1.3. Инициализируйте проект (если еще не инициализирован)
```bash
firebase init firestore
```
- Выберите ваш проект
- Выберите файл правил: `firestore.rules` (уже существует)
- Нажмите Enter для подтверждения

#### 1.4. Задеплойте правила
```bash
firebase deploy --only firestore:rules
```

#### 1.5. Проверка
- Должно появиться сообщение "Deploy complete!"
- Проверьте в Firebase Console, что правила обновились

---

## 👤 Шаг 2: Добавить поле `isSuperAdmin` в документ пользователя

### Вариант A: Через Firebase Console (Рекомендуется)

#### 2.1. Найдите ваш User ID
1. В Firebase Console перейдите в **Authentication** → **Users**
2. Найдите пользователя с email `crazymhessel@gmail.com`
3. Скопируйте **UID** (User ID) - это длинная строка типа `abc123def456...`

#### 2.2. Откройте документ пользователя
1. Перейдите в **Firestore Database**
2. Откройте коллекцию **`users`**
3. Найдите документ с ID = ваш UID (скопированный выше)
4. Нажмите на документ

#### 2.3. Добавьте поле `isSuperAdmin`
1. Нажмите кнопку **"Add field"** (Добавить поле)
2. В поле **"Field name"** введите: `isSuperAdmin`
3. В выпадающем списке **"Type"** выберите: `boolean`
4. В поле **"Value"** введите: `true`
5. Нажмите **"Update"** (или Enter)

#### 2.4. Проверка
- В документе должно появиться поле: `isSuperAdmin: true`
- Тип поля должен быть `boolean`

---

### Вариант B: Через консоль браузера (Быстро)

#### 2.1. Откройте приложение
1. Запустите ваше приложение
2. Войдите в систему под аккаунтом `crazymhessel@gmail.com`

#### 2.2. Откройте консоль браузера
1. Нажмите **F12** (или правой кнопкой → "Inspect" → "Console")
2. Перейдите на вкладку **"Console"**

#### 2.3. Выполните скрипт
Скопируйте и вставьте следующий код в консоль, затем нажмите Enter:

```javascript
(async () => {
  try {
    // Импортируем необходимые модули
    const { db, auth } = await import('./src/firebase');
    const { doc, updateDoc, getDoc } = await import('firebase/firestore');
    
    const user = auth.currentUser;
    if (!user) {
      console.error('❌ Пользователь не авторизован. Войдите в систему.');
      return;
    }
    
    console.log('👤 Текущий пользователь:', user.email);
    console.log('🆔 User ID:', user.uid);
    
    const userRef = doc(db, 'users', user.uid);
    const userDoc = await getDoc(userRef);
    
    if (!userDoc.exists()) {
      console.error('❌ Документ пользователя не найден в Firestore');
      return;
    }
    
    const userData = userDoc.data();
    console.log('📄 Текущие данные пользователя:', userData);
    
    // Проверяем, есть ли уже поле isSuperAdmin
    if (userData.isSuperAdmin === true) {
      console.log('✅ Поле isSuperAdmin уже установлено в true');
      return;
    }
    
    // Обновляем документ
    await updateDoc(userRef, {
      isSuperAdmin: true
    });
    
    console.log('✅ Поле isSuperAdmin успешно добавлено!');
    console.log('🔄 Обновите страницу (F5) и попробуйте удалить пользователя снова.');
  } catch (error) {
    console.error('❌ Ошибка:', error);
  }
})();
```

#### 2.4. Проверка
- В консоли должно появиться: `✅ Поле isSuperAdmin успешно добавлено!`
- Обновите страницу (F5)

---

### Вариант C: Использовать готовый скрипт

1. Откройте файл `scripts/setup-super-admin.js` в проекте
2. Скопируйте его содержимое
3. Вставьте в консоль браузера (F12 → Console)
4. Нажмите Enter

---

## ✅ Шаг 3: Проверка настройки

### 3.1. Проверьте правила Firestore
1. Firebase Console → Firestore → Rules
2. Убедитесь, что правила содержат функцию `isSuperAdmin()` с тремя способами проверки
3. Убедитесь, что правила опубликованы (кнопка "Publish" неактивна)

### 3.2. Проверьте документ пользователя
1. Firebase Console → Firestore → `users` → ваш документ
2. Убедитесь, что есть поле `isSuperAdmin: true` (boolean)
3. ИЛИ убедитесь, что поле `email` содержит `crazymhessel@gmail.com`

### 3.3. Проверьте в приложении
1. Обновите страницу приложения (F5)
2. Откройте консоль браузера (F12)
3. Попробуйте удалить пользователя из workspace
4. Смотрите логи в консоли:
   - Должны быть логи `[SettingsView]` и `[FirestoreService]`
   - Не должно быть ошибки "Missing or insufficient permissions"

---

## 🐛 Если проблема сохраняется

### Проверка 1: Правила действительно развернуты?
1. Firebase Console → Firestore → Rules
2. Проверьте, что правила в консоли совпадают с файлом `firestore.rules`
3. Если нет - скопируйте из файла и опубликуйте снова

### Проверка 2: Поле isSuperAdmin добавлено?
1. Firebase Console → Firestore → `users` → ваш документ
2. Проверьте наличие поля `isSuperAdmin: true`
3. Если нет - добавьте через консоль браузера (см. Шаг 2, Вариант B)

### Проверка 3: Правильный workspace?
1. Убедитесь, что вы пытаетесь удалить пользователя из правильного workspace
2. Убедитесь, что удаляемый пользователь не является OWNER

### Проверка 4: Логи в консоли
1. Откройте консоль браузера (F12)
2. Попробуйте удалить пользователя
3. Смотрите логи - они покажут, на каком шаге возникает ошибка

---

## 📝 Чек-лист

Отметьте выполненные пункты:

- [ ] Правила Firestore развернуты через Firebase Console или CLI
- [ ] Правила содержат функцию `isSuperAdmin()` с тремя способами проверки
- [ ] Поле `isSuperAdmin: true` добавлено в документ пользователя
- [ ] Страница приложения обновлена (F5)
- [ ] Попробовал удалить пользователя - работает без ошибок

---

## 🎉 Готово!

Если все пункты выполнены, удаление пользователей должно работать!

Если проблема сохраняется, проверьте логи в консоли браузера - они покажут детальную информацию об ошибке.

