// Скрипт для выдачи прав супер-администратора пользователю terehin.alv@yandex.ru
// Запустите в консоли браузера после авторизации

// Скопируйте и вставьте этот код в консоль браузера (F12) после авторизации

(async function() {
  try {
    // Динамический импорт Firebase модулей
    const { db } = await import('../src/firebase.js');
    const { collection, query, where, getDocs, updateDoc, doc } = await import('firebase/firestore');
    
    const targetEmail = 'terehin.alv@yandex.ru';
    console.log('🔍 Ищем пользователя с email:', targetEmail);
    
    // Ищем пользователя по email
    const usersRef = collection(db, 'users');
    const q = query(usersRef, where('email', '==', targetEmail));
    const querySnapshot = await getDocs(q);
    
    if (querySnapshot.empty) {
      console.error('❌ Пользователь с email', targetEmail, 'не найден в Firestore');
      console.log('💡 Убедитесь, что пользователь хотя бы раз залогинился в систему');
      console.log('💡 Если пользователь еще не залогинился, права будут работать через email в правилах Firestore');
      return;
    }
    
    // Обрабатываем найденных пользователей
    for (const userDoc of querySnapshot.docs) {
      const userData = userDoc.data();
      console.log('👤 Найден пользователь:', {
        id: userDoc.id,
        email: userData.email,
        displayName: userData.displayName,
        currentIsSuperAdmin: userData.isSuperAdmin
      });
      
      // Проверяем, есть ли уже поле isSuperAdmin
      if (userData.isSuperAdmin === true) {
        console.log('✅ Поле isSuperAdmin уже установлено в true для', targetEmail);
        console.log('✅ Пользователь уже имеет права супер-администратора');
        continue;
      }
      
      // Обновляем документ
      try {
        await updateDoc(doc(db, 'users', userDoc.id), {
          isSuperAdmin: true
        });
        
        console.log('✅ Поле isSuperAdmin успешно добавлено для', targetEmail);
        console.log('🔄 Пользователь должен перезайти в систему для применения прав');
        console.log('📋 Права супер-администратора:');
        console.log('   - Может удалять пользователей из любого workspace');
        console.log('   - Может видеть все workspace');
        console.log('   - Может выполнять все административные действия');
      } catch (error) {
        console.error('❌ Ошибка при обновлении документа:', error);
        console.error('💡 Возможно, у вас нет прав для обновления этого документа');
        console.error('💡 Попробуйте выполнить скрипт, будучи залогиненным как существующий супер-админ');
      }
    }
  } catch (error) {
    console.error('❌ Ошибка при выполнении скрипта:', error);
    console.error('💡 Убедитесь, что вы запускаете скрипт в консоли браузера после авторизации');
  }
})();

