# 🔧 Исправление ошибки демо-режима

## Проблема
При входе в демо-режим возникает ошибка:
```
Missing or insufficient permissions
```

## Причина
Правила Firestore не поддерживают анонимных пользователей (которые используются в демо-режиме). Анонимные пользователи не имеют email в токене, что вызывает ошибки при проверке прав доступа.

## Решение
Обновлены правила Firestore для поддержки анонимных пользователей.

---

## 📋 Шаг 1: Развернуть обновленные правила Firestore

### Вариант A: Через Firebase Console (Рекомендуется)

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Firestore Database** → **Rules**
3. Откройте файл `firestore.rules` в вашем проекте
4. Скопируйте **ВСЁ** содержимое (Ctrl+A, Ctrl+C)
5. Вставьте в редактор правил в Firebase Console (Ctrl+V)
6. Нажмите **"Publish"** (Опубликовать)
7. Подождите несколько секунд

### Вариант B: Через Firebase CLI

```bash
firebase deploy --only firestore:rules
```

---

## ✅ Что было исправлено

### 1. Правила для создания пользователей (`users/{userId}`)
- **Было:** Требовался email в токене для создания документа пользователя
- **Стало:** Разрешено создание для анонимных пользователей (email может быть `null` или `demo@example.com`)

### 2. Правила для чтения workspace
- **Было:** Проверка `allowedEmails` могла упасть, если email в токене был `null`
- **Стало:** Проверка `allowedEmails` выполняется только если email в токене не `null`

### 3. Правила для обновления пользователей
- **Было:** Требовалось точное совпадение email
- **Стало:** Разрешено обновление для анонимных пользователей с email `demo@example.com`

---

## 🧪 Проверка

1. Обновите страницу приложения (F5)
2. Нажмите "Войти в демо-режим"
3. Должно работать без ошибок! ✅

---

## 📝 Изменения в правилах

### Файл: `firestore.rules`

#### Изменение 1: Создание пользователей
```javascript
// БЫЛО:
allow create: if request.auth != null && 
                 request.auth.uid == userId &&
                 request.resource.data.id == userId &&
                 request.resource.data.email == request.auth.token.email;

// СТАЛО:
allow create: if request.auth != null && 
                 request.auth.uid == userId &&
                 request.resource.data.id == userId &&
                 // Для анонимных пользователей email в токене может быть null
                 (request.auth.token.email == null || 
                  request.resource.data.email == request.auth.token.email ||
                  request.resource.data.email == 'demo@example.com');
```

#### Изменение 2: Обновление пользователей
```javascript
// БЫЛО:
allow update: if request.auth != null && 
                 request.auth.uid == userId &&
                 resource.data.id == request.resource.data.id &&
                 resource.data.email == request.resource.data.email;

// СТАЛО:
allow update: if request.auth != null && 
                 request.auth.uid == userId &&
                 resource.data.id == request.resource.data.id &&
                 // Для анонимных пользователей разрешаем обновление email
                 (resource.data.email == request.resource.data.email ||
                  resource.data.email == null ||
                  request.resource.data.email == 'demo@example.com');
```

#### Изменение 3: Чтение workspace
```javascript
// БЫЛО:
allow read: if request.auth != null && 
  (
    isSuperAdmin() ||
    resource.data.ownerId == request.auth.uid || 
    resource.data.allowedEmails.hasAny([request.auth.token.email]) ||
    exists(...)
  );

// СТАЛО:
allow read: if request.auth != null && 
  (
    isSuperAdmin() ||
    resource.data.ownerId == request.auth.uid || 
    // Проверяем allowedEmails только если email в токене не null
    (request.auth.token.email != null && 
     resource.data.allowedEmails.hasAny([request.auth.token.email])) ||
    exists(...)
  );
```

---

## 🐛 Если проблема сохраняется

### Проверка 1: Правила развернуты?
1. Firebase Console → Firestore → Rules
2. Проверьте, что правила содержат поддержку анонимных пользователей
3. Убедитесь, что правила опубликованы (кнопка "Publish" неактивна)

### Проверка 2: Логи в консоли
1. Откройте консоль браузера (F12)
2. Попробуйте войти в демо-режим
3. Смотрите логи - они покажут, на каком шаге возникает ошибка

### Проверка 3: Анонимная аутентификация включена?
1. Firebase Console → Authentication → Sign-in method
2. Убедитесь, что **Anonymous** включен
3. Если нет - включите и сохраните

---

## ✅ Чек-лист

- [ ] Правила Firestore развернуты через Firebase Console или CLI
- [ ] Правила содержат поддержку анонимных пользователей
- [ ] Анонимная аутентификация включена в Firebase Console
- [ ] Страница приложения обновлена (F5)
- [ ] Демо-режим работает без ошибок

---

## 🎉 Готово!

После развертывания обновленных правил демо-режим должен работать корректно!

