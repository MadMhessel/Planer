# Исправление проблемы с входом через Google

## Проблема
При нажатии на кнопку "Войти через Google" страница обновляется или перенаправляет на пустую страницу localhost.

## Решение

### 1. Проверьте настройки Firebase Console

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **Settings** → **Authorized domains**
4. Убедитесь, что добавлены следующие домены:
   - `localhost` (для локальной разработки)
   - Ваш домен Cloud Run (например: `command-task-planner-883668243599.us-west1.run.app`)
   - Любые другие домены, где вы используете приложение

### 2. Включите Google Authentication

1. В Firebase Console перейдите в **Authentication** → **Sign-in method**
2. Убедитесь, что **Google** включен как метод входа
3. Если не включен, нажмите на Google и включите его
4. Укажите email поддержки (опционально)

### 3. Проверьте конфигурацию Firebase

Убедитесь, что в `.env` или в переменных окружения при деплое установлены все необходимые переменные:

```env
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
```

### 4. Проверьте консоль браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Console**
3. Попробуйте войти через Google
4. Проверьте сообщения об ошибках:
   - `auth/unauthorized-domain` - домен не авторизован
   - `auth/popup-blocked` - всплывающее окно заблокировано
   - `auth/popup-closed-by-user` - окно закрыто пользователем
   - `auth/operation-not-allowed` - метод не включен

### 5. Разрешите всплывающие окна

Если браузер блокирует всплывающие окна:
- Chrome: Нажмите на иконку блокировки в адресной строке → Разрешить всплывающие окна
- Firefox: Настройки → Приватность и защита → Разрешения → Всплывающие окна
- Safari: Настройки → Веб-сайты → Всплывающие окна

### 6. Проверьте CORS настройки

Если вы используете Cloud Run, убедитесь, что:
- Домен добавлен в Firebase Authorized domains
- Нет проблем с CORS (Firebase обычно обрабатывает это автоматически)

## Что было исправлено в коде

1. ✅ Добавлена обработка ошибок в `AuthService.loginWithGoogle()`
2. ✅ Добавлено логирование для отладки
3. ✅ Добавлено отображение ошибок в UI
4. ✅ Добавлено состояние загрузки при аутентификации
5. ✅ Добавлена проверка конфигурации Firebase при загрузке

## Тестирование

После исправления настроек:

1. Откройте приложение
2. Откройте консоль браузера (F12)
3. Нажмите "Войти через Google"
4. Проверьте сообщения в консоли:
   - Должно появиться: `🔐 Начало аутентификации через Google...`
   - При успехе: `✅ Аутентификация успешна: your-email@gmail.com`
   - При ошибке: `❌ Ошибка аутентификации: [детали ошибки]`

## Если проблема сохраняется

1. Проверьте логи в Cloud Run:
   ```bash
   gcloud run services logs read command-task-planner --limit 50 --region us-west1
   ```

2. Проверьте, что переменные окружения правильно установлены в Cloud Run:
   ```bash
   gcloud run services describe command-task-planner --region us-west1 --format="value(spec.template.spec.containers[0].env)"
   ```

3. Убедитесь, что домен Cloud Run добавлен в Firebase Authorized domains

4. Попробуйте очистить кеш браузера и cookies

5. Проверьте, что OAuth consent screen настроен в Google Cloud Console (если требуется)

