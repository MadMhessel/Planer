# 🚀 Быстрый старт - Исправление ошибки удаления пользователей

## ⚡ За 2 минуты

### Шаг 1: Развернуть правила Firestore (1 минута)

1. Откройте [Firebase Console](https://console.firebase.google.com/) → ваш проект
2. Перейдите в **Firestore Database** → **Rules**
3. Откройте файл `firestore.rules` в проекте
4. Скопируйте **ВСЁ** содержимое (Ctrl+A, Ctrl+C)
5. Вставьте в редактор правил в Firebase Console (Ctrl+V)
6. Нажмите **"Publish"**
7. Подождите несколько секунд

✅ **Готово!**

---

### Шаг 2: Добавить поле isSuperAdmin (1 минута)

**Вариант A: Через консоль браузера (быстрее)**

1. Откройте приложение и войдите в систему
2. Нажмите **F12** → вкладка **Console**
3. Скопируйте и вставьте этот код:

```javascript
(async () => {
  const { db, auth } = await import('./src/firebase');
  const { doc, updateDoc, getDoc } = await import('firebase/firestore');
  const user = auth.currentUser;
  if (!user) { console.error('Войдите в систему!'); return; }
  const userRef = doc(db, 'users', user.uid);
  const userDoc = await getDoc(userRef);
  if (!userDoc.exists()) { console.error('Документ не найден'); return; }
  if (userDoc.data().isSuperAdmin === true) { console.log('✅ Уже установлено'); return; }
  await updateDoc(userRef, { isSuperAdmin: true });
  console.log('✅ Готово! Обновите страницу (F5)');
})();
```

4. Нажмите **Enter**
5. Обновите страницу (F5)

✅ **Готово!**

---

**Вариант B: Через Firebase Console**

1. Firebase Console → **Firestore Database** → **users**
2. Найдите документ с вашим User ID (из Authentication → Users)
3. Нажмите **"Add field"**
4. **Field name**: `isSuperAdmin`
5. **Type**: `boolean`
6. **Value**: `true`
7. Нажмите **"Update"**

✅ **Готово!**

---

## ✅ Проверка

1. Обновите страницу приложения (F5)
2. Попробуйте удалить пользователя из workspace
3. Должно работать без ошибок! 🎉

---

## 📚 Подробные инструкции

Если что-то не работает, смотрите:
- `SETUP_INSTRUCTIONS.md` - полная пошаговая инструкция
- `SETUP_SUPER_ADMIN_SCRIPT.md` - скрипты для настройки
- `FIREBASE_CLI_SETUP.md` - настройка через CLI

---

## 🐛 Проблемы?

### Ошибка: "Missing or insufficient permissions"
- ✅ Правила Firestore развернуты? (Шаг 1)
- ✅ Поле `isSuperAdmin: true` добавлено? (Шаг 2)
- ✅ Страница обновлена? (F5)

### Ошибка: "Документ пользователя не найден"
- Документ создается автоматически при первом входе
- Или создайте вручную через Firebase Console

### Ошибка: "Пользователь не авторизован"
- Войдите в систему в приложении
- Убедитесь, что используете аккаунт `crazymhessel@gmail.com`

