# 📖 Полная инструкция по настройке

## 🎯 Проблема
Ошибка "Missing or insufficient permissions" при попытке удалить пользователя из workspace.

## ✅ Решение
Нужно выполнить 2 шага:
1. Развернуть правила Firestore
2. Добавить поле `isSuperAdmin: true` в документ пользователя

---

## 📚 Документы с инструкциями

### 🚀 Быстрый старт
- **`QUICK_START.md`** - настройка за 2 минуты (рекомендуется начать отсюда)

### 📋 Подробные инструкции
- **`SETUP_INSTRUCTIONS.md`** - полная пошаговая инструкция с скриншотами
- **`SETUP_SUPER_ADMIN_SCRIPT.md`** - скрипты для автоматической настройки
- **`FIREBASE_CLI_SETUP.md`** - настройка через Firebase CLI

### 🔍 Диагностика
- **`CODE_ANALYSIS.md`** - анализ кода и последовательности
- **`PROBLEM_DIAGNOSIS.md`** - выводы и решение
- **`DEBUG_CHECKLIST.md`** - чек-лист для диагностики

---

## 📁 Готовые файлы

### Правила Firestore
- **`firestore.rules`** - готовые правила для загрузки в Firebase Console

### Скрипты
- **`scripts/setup-super-admin.js`** - скрипт для добавления `isSuperAdmin` через консоль браузера

---

## 🎯 Рекомендуемый порядок действий

1. **Начните с `QUICK_START.md`** - быстрая настройка за 2 минуты
2. Если не работает - смотрите **`SETUP_INSTRUCTIONS.md`** - подробная инструкция
3. Если все еще не работает - смотрите **`DEBUG_CHECKLIST.md`** - диагностика

---

## ⚡ Быстрая справка

### Развернуть правила Firestore
1. Firebase Console → Firestore → Rules
2. Скопируйте содержимое `firestore.rules`
3. Вставьте и нажмите "Publish"

### Добавить isSuperAdmin
**Через консоль браузера:**
```javascript
(async () => {
  const { db, auth } = await import('./src/firebase');
  const { doc, updateDoc, getDoc } = await import('firebase/firestore');
  const user = auth.currentUser;
  if (!user) return;
  const userRef = doc(db, 'users', user.uid);
  const userDoc = await getDoc(userRef);
  if (userDoc.exists() && userDoc.data().isSuperAdmin !== true) {
    await updateDoc(userRef, { isSuperAdmin: true });
    console.log('✅ Готово!');
  }
})();
```

**Через Firebase Console:**
1. Firestore → `users` → ваш документ
2. Add field: `isSuperAdmin` (boolean) = `true`

---

## ✅ Чек-лист

- [ ] Правила Firestore развернуты
- [ ] Поле `isSuperAdmin: true` добавлено
- [ ] Страница обновлена (F5)
- [ ] Удаление пользователя работает

---

## 🆘 Помощь

Если проблема сохраняется:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте правила в Firebase Console
3. Проверьте документ пользователя в Firestore
4. Смотрите `DEBUG_CHECKLIST.md` для детальной диагностики

