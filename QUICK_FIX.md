# 🚀 Быстрое решение проблемы с удалением пользователей

## Проблема
Не удается удалить пользователя из workspace, ошибка "Missing or insufficient permissions".

## Решение (3 шага)

### Шаг 1: Разверните обновленные правила Firestore

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Firestore Database** → **Rules**
3. Скопируйте **ВСЁ** содержимое файла `firestore.rules` из проекта
4. Вставьте в редактор правил в Firebase Console
5. Нажмите **"Publish"** (Опубликовать)
6. Подождите несколько секунд

### Шаг 2: Добавьте поле `isSuperAdmin` в ваш документ пользователя

**Вариант A: Через Firebase Console (проще)**

1. В Firebase Console перейдите в **Firestore Database**
2. Откройте коллекцию `users`
3. Найдите документ с вашим `userId` (можно найти через **Authentication** → **Users** → скопировать UID)
4. Нажмите на документ
5. Нажмите **"Add field"** (Добавить поле)
6. Добавьте:
   - **Field name**: `isSuperAdmin`
   - **Type**: `boolean` (булево)
   - **Value**: `true`
7. Нажмите **"Update"**

**Вариант B: Через консоль браузера**

1. Откройте приложение и войдите в систему
2. Откройте консоль браузера (F12)
3. Вставьте и выполните следующий код:

```javascript
import { db, auth } from './src/firebase';
import { doc, updateDoc, getDoc } from 'firebase/firestore';

const user = auth.currentUser;
if (!user) {
  console.error('Войдите в систему!');
} else {
  const userRef = doc(db, 'users', user.uid);
  const userDoc = await getDoc(userRef);
  if (userDoc.exists()) {
    await updateDoc(userRef, { isSuperAdmin: true });
    console.log('✅ Готово! Обновите страницу.');
  }
}
```

### Шаг 3: Проверьте

1. Обновите страницу приложения (F5)
2. Попробуйте удалить пользователя из workspace
3. Должно работать! ✅

## Если не помогло

Смотрите подробные инструкции в файлах:
- `DIAGNOSTIC_CHECK.md` - полная диагностика
- `SETUP_SUPER_ADMIN.md` - детальная настройка

## Важно

- Убедитесь, что правила Firestore **опубликованы** (кнопка "Publish" в Firebase Console)
- Убедитесь, что поле `isSuperAdmin: true` добавлено в ваш документ пользователя
- Обновите страницу после изменений


