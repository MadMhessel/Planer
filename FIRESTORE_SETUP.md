# 🔧 Настройка Firestore для работы приложения

## ⚠️ Важно: Правила Firestore должны быть задеплоены!

Без правильных правил Firestore создание задач и проектов не будет работать из-за ошибки "Missing or insufficient permissions".

---

## 📋 Шаг 1: Деплой правил Firestore

### Вариант A: Через Firebase Console (Рекомендуется)

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект
3. Перейдите в **Firestore Database** → **Rules**
4. Скопируйте содержимое файла `firestore.rules` из проекта
5. Вставьте в редактор правил
6. Нажмите **"Publish"** (Опубликовать)

### Вариант B: Через Firebase CLI

```bash
# Установите Firebase CLI (если еще не установлен)
npm install -g firebase-tools

# Войдите в Firebase
firebase login

# Инициализируйте проект (если еще не инициализирован)
firebase init firestore

# Задеплойте правила
firebase deploy --only firestore:rules
```

---

## 📋 Шаг 2: Создание индексов для collection group queries

Для работы подписки на workspace (чтобы находить workspace, где пользователь является участником) нужен индекс.

### Автоматическое создание (Рекомендуется)

1. Попробуйте использовать приложение
2. Если появится ошибка с ссылкой на создание индекса - нажмите на ссылку
3. Firebase автоматически создаст нужный индекс

### Ручное создание

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Firestore Database** → **Indexes**
3. Нажмите **"Create Index"**
4. Настройте индекс:
   - **Collection ID**: `members` (collection group)
   - **Fields to index**:
     - `userId` (Ascending)
     - `status` (Ascending)
   - **Query scope**: Collection group
5. Нажмите **"Create"**

---

## 📋 Шаг 3: Проверка правил

После деплоя правил проверьте:

1. Откройте консоль браузера (F12)
2. Попробуйте создать задачу или проект
3. Если ошибка "Missing or insufficient permissions" - проверьте:
   - Правила задеплоены (Firebase Console → Firestore → Rules)
   - Пользователь является членом workspace (проверьте в Firestore Console)
   - `workspaceId` передается корректно (проверьте в логах консоли)

---

## 🔍 Проверка членства в workspace

Чтобы проверить, является ли пользователь членом workspace:

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Firestore Database**
3. Откройте `workspaces` → выберите ваш workspace → `members`
4. Убедитесь, что там есть документ с вашим `userId` (из Firebase Auth)
5. Проверьте, что `status` = `'ACTIVE'`

---

## 🐛 Отладка проблем

### Ошибка: "Missing or insufficient permissions"

**Причины:**
1. Правила Firestore не задеплоены
2. Пользователь не является членом workspace
3. `workspaceId` не передается или равен `null`

**Решение:**
1. Проверьте, что правила задеплоены в Firebase Console
2. Проверьте членство в workspace (см. выше)
3. Откройте консоль браузера (F12) и проверьте логи:
   ```javascript
   // Должны быть логи вида:
   // "Creating project" { workspaceId: "...", name: "..." }
   // или
   // "Creating task" { workspaceId: "...", title: "..." }
   ```

### Ошибка: "Index required"

**Причина:** Нужен индекс для collection group query

**Решение:** Создайте индекс (см. Шаг 2)

---

## ✅ Чек-лист настройки

- [ ] Правила Firestore задеплоены через Firebase Console или CLI
- [ ] Индекс для `members` collection group создан (если используется)
- [ ] Пользователь является членом workspace (проверено в Firestore Console)
- [ ] В консоли браузера нет ошибок при создании задач/проектов
- [ ] Логи показывают корректные `workspaceId` и другие данные

---

## 📝 Быстрая команда для проверки правил

```bash
# Проверить текущие правила (если установлен Firebase CLI)
firebase firestore:rules:get
```

---

## 🔗 Полезные ссылки

- [Firebase Console](https://console.firebase.google.com/)
- [Документация Firestore Rules](https://firebase.google.com/docs/firestore/security/get-started)
- [Документация по индексам](https://firebase.google.com/docs/firestore/query-data/indexing)

